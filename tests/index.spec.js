describe( 'findSiblings', () => {

	it ( 'should not contain the node arg', () => {

		let siblings = findSiblings()


	} )

	it ( 'should be castable to a NodeList or HTMLCollection', () => {} )

} )